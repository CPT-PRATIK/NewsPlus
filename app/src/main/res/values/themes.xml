<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NewsPlus" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/one</item>
        <item name="colorPrimaryVariant">@color/two</item>
        <item name="android:statusBarColor">@color/three</item>
    </style>

    <style name="Theme.NewsPlus" parent="Base.Theme.NewsPlus" />

    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/four</item>
    </style>
</resources>